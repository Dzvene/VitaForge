package net.matrixcapital.vitaforge.core

/** Backend host + API prefix. Production API is the source of truth (MOBILE_API.md). */
object ApiConfig {
    const val BASE_URL = "https://api.vita-forge.app/api/v1"
}
