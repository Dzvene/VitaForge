"""Recipes / meals — a named set of foods the user can log in one tap.

Logging a recipe expands to one diary entry per component (via DiaryService),
so the diary, day summary and analytics keep working unchanged.
"""
